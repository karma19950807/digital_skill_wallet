import { ParseArtefactLinkPipe } from './parse-artefact.pipe';

describe('ParseArtefactLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseArtefactLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
